Servidor em modo Server Core

 configurações

    sconfig (aparecera as opções em um modo inteligente)
    
    ctrl + shift + esc (caso fechar a linha de comando)
   
    Para chamar o powe shell e so chamar 'powershell'

    Get WindowsFeature (mostra todas instações na maquina)



Subindo AD replica com o Server Core - Controlador de dominio
  

  O Ad replica é um controlador de dominio que atende o mesmo dominio que o outro controlador de dominio, sendo assim, tudo que é feito em um, é replicado no outro.


 comandos:

  IMAGENS 


Coleta de informações por linha de comando - sercer core

  # Verifica o fqdn do dominio 
1. whoami /fqdn

 # Mostra uma lista de controladores do dominio e seus site
2. nltest /dclist:[nome_dominio]

 # Mostra quem é o controlador principal dentro da estrutura
3. nltest /dsgetdc:[nome_dominio]

 # Lista dominios via nslookup
4.nslookup -tyoenany %userdnsdomain

 # Valida sessoes ativas
5. query user /server:SRV-01


controlador de dominio - Interface grafica - Montando um subdominio


 
   Trocar o ip da maquina para um ip livre 


 Teste de ping 
   ping [ip_que_deseja]

 Teste de arp
    arp -a  

testa conectividade com a rede

  | Sucesso


Gerenciador de servidor > adicionar funções e recursos > proximo > instalação baseada em função e recursos  > selecionar um servidor no pool de servidor > servicos de Dominio Active Directory > adicionar recursos > proximo > proximo > proximo > instalar


Aba de notificações > promover servidor a um controlador de dominio > adicionar um novo dominio a uma floresta existente > inserir as informações e proximo > inserir as informações e deixar habilitado "Servidor do sistema" e "Catalogo global"> proximo > proximo > proximo > proximo > instalar 



Verificar os papeis de floresta 

  netdom query fsmo   





  

https://www.youtube.com/watch?v=2NolRXdVUFg
https://www.youtube.com/watch?v=4GIEp_cbSeQ